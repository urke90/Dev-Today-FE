export enum EQueryType {
  POST = 'post',
  MEETUP = 'meetup',
  PODCAST = 'podcast',
  GROUP = 'group',
  MEMBERS = 'members',
}

export enum ESortByFilter {
  RECENT = 'recent',
  POPULAR = 'popular',
  JOINED = 'joined',
  FOLLOWING = 'following',
}
