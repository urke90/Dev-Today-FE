import ProfilePageLoader from '@/components/shared/Loaders/ProfilePageLoader';

// ----------------------------------------------------------------

const loading = () => {
  return (
    <section className="px-3.5 lg:px-5">
      <ProfilePageLoader />
    </section>
  );
};

export default loading;
