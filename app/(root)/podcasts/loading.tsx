import PodcastsPageLoader from '@/components/shared/Loaders/PodcastsPageLoader';

// ----------------------------------------------------------------

const Loading = () => {
  return (
    <section className="px-3.5 lg:px-5">
      <PodcastsPageLoader />
    </section>
  );
};

export default Loading;
